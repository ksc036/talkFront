export enum RunToppingMessageType {
  NAVIGATE_TARGET_ROOM,
  NAVIGATE_CONTENT_ROOM,
  NAVIGATE_SHOW_CONTENT,
}

export enum AppType {
  DOCSDRIVE,
  TALKDRIVE,
}

export enum FileOpenType {
  SUBAPP,
  HANCOMOFFICE,
}
