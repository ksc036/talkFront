import { styled } from '@wapl/ui';

export const ButtonWrapper = styled('div')`
  display: flex;
  justify-content: center;
  margin: 0 70px;
  margin-top: auto;
  text-align: center;
  button {
    flex: 1;
  }
  gap: 12px;
`;
